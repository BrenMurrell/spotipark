// import { render, screen } from '@testing-library/react'
// import App from '../App'

describe('App', () => {
  it.todo('renders the App component');
  // TODO: need to implement testing for spotify SDK first
  // it('renders the App component', () => {
  //   render(<App />)
  //   expect(screen.getByRole('heading', { level: 1}).textContent).toBe('SpotiPark');
  //   expect(screen.getByRole('heading', { level: 2}).textContent).toBe('You are not logged in');
  // })
})
